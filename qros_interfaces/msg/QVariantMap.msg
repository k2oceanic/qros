# Corresponds to Qt's QVariantMap (QMap<QString, QVariant>).
# Represented as parallel arrays since ROS 2 has no native map type.
# keys[i] maps to values[i]; both arrays must be the same length.
string[] keys
QVariant[] values
